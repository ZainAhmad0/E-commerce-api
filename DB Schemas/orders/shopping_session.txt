CREATE TABLE SHOPPING_SESSION (
ID VARCHAR(100) NOT NULL,
USER_ID VARCHAR(100) NOT NULL,
PRIMARY KEY (ID),
CONSTRAINT FK_USER_SHOPPING_SESSION FOREIGN KEY (USER_ID) REFERENCES USER_INFO (ID) ON
DELETE
	NO ACTION ON
	UPDATE
		NO ACTION );