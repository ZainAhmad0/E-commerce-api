CREATE TABLE CART_ITEM ( ID VARCHAR(100) NOT NULL,
SESSION_ID VARCHAR(100) NOT NULL,
PRODUCT_ID VARCHAR(100) NOT NULL,
QUANTITY INT NOT NULL,
PRIMARY KEY (ID),
CONSTRAINT FK_SHOPPING_SESSION FOREIGN KEY (SESSION_ID) REFERENCES shopping_session (id) ON
DELETE
	NO ACTION ON
	UPDATE
		NO ACTION,
		CONSTRAINT FK_Product FOREIGN KEY (PRODUCT_ID) REFERENCES product (id) ON
		DELETE
			NO ACTION ON
			UPDATE
				NO ACTION );